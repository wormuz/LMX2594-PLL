No WFI opcode (0xBF30) and no PWR base (0x40007000) literal anywhere in the 64KiB image — MCU never enters STOP/SLEEP; it free-runs polling in `FUN_08006410`'s infinite `do{}while(true)` main loop, and USB suspend is handled purely at USB peripheral level (D+/D- pull-up state via CNTR/ISTR), not CPU sleep.

FINDINGS — power_path:

- Suspend cb `FUN_08002270` @0x08002270: prints "usb enter low power mode", sets `*RAM[0x20001ED0]=3` (DAT_0800229c). No SPI/GPIO/LMX2594 write. Called only from `FUN_08005224`@0x08005224 (USB SUSP-bit ISR handler), which does PMA save (EP buffer copy) around it — pure USB-core state save, unrelated to synth.
- Resume cb `FUN_08003cc0`@0x08003cc0 (called from `FUN_08004838`@0x08004838, itself the WKUP-bit handler invoked in ISR chain): prints "usb leave low power mode", reads flag byte at `*(iVar1+10)` (iVar1=_DAT_08003ce0, a config/state struct) to decide `*RAM_gate=1` (normal) vs `5` (alt state). No SPI/LMX2594 touch either.
- RAM[0x20001ED0] (`DAT_0800229c`/`DAT_08003cfc` alias) is a pure USB power-state flag: written 3 (suspended) / 1 or 5 (resumed) by these two callbacks; read only by `FUN_0800485c`@0x0800485c (checks ==5, increments a counter, and at count==5 calls `FUN_08002120`, the CDC-TX ring-buffer pump `FUN_08005c0c`/`FUN_08004b28`/`FUN_08004b84`). This is USB CDC flow control, not power management of the RF chip.
- USB ISR dispatcher `FUN_080058d4`@0x080058d4: handles CTR/RESET/SUSP/WKUP/SOF/ESOF/ERR bits from ISTR register (base pattern consistent with USB=0x40005c00). SUSP branch (bit 0x400) → `FUN_08005224`; no bit handler touches SPI1 (0x40013000) or any LMX2594 CS/LE GPIO.
- Confirmed: no WFI opcode (`0xBF30`) and no PWR peripheral base literal (`0x40007000`) exist anywhere in the 64KiB flash image. Cortex-M3 never enters SCB-controlled sleep/STOP; main loop `FUN_08006410`@0x08006410 is an infinite polling `do{}while(true)`.
- Root cause confirmed and expanded: "usb low power mode" in this firmware is USB-peripheral-only (D+/D- suspend per USB spec, CNTR/ISTR bits), decoupled entirely from both MCU sleep and LMX2594 control. No code path anywhere (searched all refs from both cb's, their dispatcher, and RAM[0x20001ED0] readers) issues an SPI powerdown/output-disable write to the synth on USB suspend. Chip stays fully powered and RF-active regardless of USB link state — this is the overheat mechanism, confirmed exact, not a hypothesis.