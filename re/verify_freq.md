All four claims CONFIRMED with decompiled/disassembled evidence from `firmware.bin` around `FUN_08005f50` (0x08005F50-0x08006024) and boot init at 0x08002c30.

Key addresses verified: R0 written only once, at boot init sequence (0x08002bd0-0x08002c2c region), never inside FUN_08005f50 or any of its 9 call sites (FUN_08003480, FUN_08003b3c, FUN_080040a8 x2, FUN_08006410 x4, plus sweep-step call). R34 written exactly once via `mov.w r0,#0x220000` at 0x08002c30 — that exact instruction encoding occurs nowhere else in the image. The R36 write `FUN_080069f4(uVar3 | 0x240000)` has no `& 0xffff` mask on `uVar3` in the decompile. The bracket-matching logic (17 CHDIV thresholds) leaves `bVar1=false` with no reject path — the `else` branch still programs R45/R46 (VCO-direct mux) and proceeds through the rest of the register writes unconditionally.